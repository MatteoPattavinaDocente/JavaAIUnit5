package it.epicode.demo.notifiche.dto;

/**
 * Il contenuto minimo per le prove: un campo di testo.
 * Serve solo a verificare che il giro funzioni, prima di metterci dentro il dominio.
 */
public record TestMessage(String text) {
}
